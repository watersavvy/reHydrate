
<div class="row">
  <div class="col-sm-offset-1 col-sm-10 col-md-offset-2 col-md-8">
    <div class="panel panel-default">
      <div class="panel-heading">
        <h3 class="panel-title">Location #<%= @refill.id %></h3>
      </div>
      <div class="panel-body">
        <div class="row">
          <div class="col-md-12">
            <dl class="dl-horizontal">

            <!--   // attempted to add name // -->

              <dt>Address</dt>
              <dd><%= @refill.address %></dd>

              <dt>Techology</dt>
              <dd><%= @refill.tech %></dd>

              <dt>Cost Per Gallon</dt>
              <dd><%= @refill.cost %></dd>


            </dl>
            <div class="btn-group btn-group-justified">
              <%= link_to icon('chevron-left'), root_path, class: "btn btn-primary" %>

              <!-- <#%= link_to icon('edit'), edit_refill_path(@refill), class: "btn btn-warning" %>

              <#%= link_to icon('trash-o'), @refill, method: :delete, data: { confirm: "Do you really want to delete this location?" }, class: "btn btn-danger" %> -->
            </div>
          </div>
        </div>
      </div>
    </div>
  </div>
</div>

<!-- End responsive panel example